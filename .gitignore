.env
.gradio
__pycache__/

test_faq_progress_*.json
test_faq_results_*.json

# Local agent/tooling scratch — never part of the repo.
.gstack/
.context/

# Resume checkpoints of the classification runs — reproducible working files,
# regenerated by scripts/assignment.py. The RESULT (data/assignment-*.json and
# the report) is checked in; the per-batch scratch is not.
data/quality_runs/
